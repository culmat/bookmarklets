/**
 * @title Edit This Page
 * @description Toggles `document.designMode`, turning the page into an editable
 *              document. Handy for mocking up copy changes or blanking out
 *              sensitive text before a screenshot. Nothing is saved — reload to undo.
 * @order 20
 */
document.designMode = document.designMode === "on" ? "off" : "on";
